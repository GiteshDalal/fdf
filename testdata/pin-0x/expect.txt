exit: 1
contains: FAIL: INDEX.md: fdf_version "0.7" is not a supported version (1.0) — upgrading the bundle is the user's decision, since `fdf migrate` moves its documents and rewrites references to them across the project: `fdf migrate --dry-run` shows the plan (F1)
contains: Bundle not validated
not-contains: (F3)
