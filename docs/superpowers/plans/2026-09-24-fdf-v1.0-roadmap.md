# FDF 1.0 — delivery roadmap

**Spec:** [2026-09-24-fdf-v1.0-design.md](../specs/2026-09-24-fdf-v1.0-design.md)

The 1.0 design spans five subsystems that depend on each other in order: the
foundations, the validator, the commands, `fdf migrate`, and the install,
skills and documentation. It is delivered as five plans, each written in full
once the one before it has landed, because each plan builds on the exact APIs
its predecessor creates.

The plans **expand, then contract**. Plans 1 to 4 add 1.0 beside 0.x, so every
step leaves the tests green; Plan 5 removes 0.x and ships. No release is cut
from the integration branch until Plan 5 is done.

## Branches

- **`v1`** is the integration branch, created from `claude/v1-0-design` (which
  holds the design, this roadmap and the plans) once they are reviewed.
- **Each plan** is worked on a branch off `v1` and merged into `v1` by pull
  request, once CI passes (`go vet ./...`, `go test ./...`, `gofmt -l .`
  printing nothing, and the skills' frontmatter lint).
- **0.7.1** fixes `fdf mv` for everyone still on 0.7, and is released from
  `main`. Plan 1 is done, so 0.7.1 is five of its commits cherry-picked onto a
  branch off `main`: ff7e809 (the `links` engine), 40635f6 (`fdf mv` on it),
  51abc22 (`fdf log` reads the pin as the validator does), 20bce8f
  (angle-bracket destinations) and 1546a13 (a listing found by its first
  link). None needs `specver`. Only CLAUDE.md conflicts, on ff7e809: put the
  `links` bullet after the `fdfroot` bullet. They fix these defects in 0.7.0:
  - links that leave the bundle break on a move that changes depth;
  - a footnote's first word is rewritten as a path;
  - a reference definition inside a code fence is rewritten;
  - on a moved index listing line, URLs, anchors and mail links are rewritten
    as paths;
  - a destination written as `<path>` loses its opening bracket to the path;
  - a listing line that links on to other documents stays behind when its
    document changes group, and another document's listing that mentions the
    moved one is carried off in its place;
  - `fdf log` refuses a draft's log under a single-quoted pin that
    `fdf validate` accepts.

  When 0.7.1 lands, merge `main` into `v1`. Only CLAUDE.md conflicts: keep
  `v1`'s side.
- **When Plan 5 is done**, `v1` merges into `main` and `v1.0.0` is tagged
  there.

## The plans

| # | Plan | Delivers | Needs |
|---|---|---|---|
| 1 | [Foundations](2026-09-24-fdf-v1.0-plan-1-foundations.md) | `specver` (MAJOR.MINOR versions, compared by number); `links`, the one link-repair engine; `fdf mv` on the engine, which fixes its broken links on moves that change depth | — |
| 2 | [Validator learns 1.0](2026-09-24-fdf-v1.0-plan-2-validator.md) | `spec/1.0.md`; the `layout` package; `1.0` among the supported pins; for a 1.0 pin, positions from `layout` (the closed root, `features/`, nested groups, flat features, the directory rule), the did-you-mean hint (F10, F14), `features` among F12's register names, and links read by `links.Find`; the 1.0 fixtures. 0.x validation unchanged | 1 |
| 3 | [Commands write 1.0](2026-09-25-fdf-v1.0-plan-3-commands.md) | `CurrentVersion` 1.0 and `fdf init`; root resolution (`docs/fdf/`, and also `docs/features/`); `new`, `adopt`, `practice`, `debt`, `bug`, `change`, `fix` on `layout`, with nested groups and name clashes refused; full feature IDs in `--affects` and `history`; `mv`, `lexicon`, `log`, `release`, the registers' `--cleanup` and `adopt`'s map on `layout`; `help` and `spec`. Commands point a 0.x bundle at `fdf migrate` | 2 |
| 4 | [`fdf migrate` 0.x → 1.0](2026-09-25-fdf-v1.0-plan-4-migrate.md) | Design §6 in full: the plan computed first, `--dry-run`, feature groups into `features/`, IDs, links, indexes, pin, relocation and `--to`, references outside the bundle, the refusals, the log, the report and the next steps | 1, 2, 3 |
| 5 | [Contract and ship](2026-09-26-fdf-v1.0-plan-5-ship.md) | The validator drops 0.x (version gates removed; the 0.x fixtures converted with migrate or removed; the pin required; the soft-check changes); `fdf install` (the primer, `legacyPrimers`, recognizing the root recorded in `.fdf-version`); the ten skills; `README.md`, `SPEC.md`, `spec/README.md`, this repository's `CLAUDE.md` and `embed.go`; acceptance on a scratch clone of ginee; release 1.0.0 | 4 |

## Where each part of the design lands

| Design section | Plan |
|---|---|
| §1 Layout, §2 IDs | 2 (validator), 3 (commands) |
| §3 Location and root resolution | 3 |
| §4 Versioning | 1 (comparison by number), 2 (1.0 pin), 5 (0.x dropped, pin required) |
| §5 Link repair | 1 (engine, `fdf mv`), 2 (the validator's link check) |
| §6 `fdf migrate` | 4 |
| §7 CLI | 2 (`layout`), 3 (commands), 5 (`install`) |
| §8 Skills and docs | 2 (`spec/1.0.md`), 5 (everything else) |
| Rule changes | 2 (for 1.0 pins), 5 (for every pin) |
| Testing | each plan its own; 5 converts the fixtures and runs the ginee acceptance |

## Carried forward from Plan 1

Plan 1's reviews left these for the plans that own them.

Plan 2 did its items: `links.Find` reads every listed form, `links.Resolve` is
exported and shared, `bundle.pinAtLeast` takes a `specver.Version`, and every
`supportedVersions` key is tested to parse.

Plan 3 did its items: the commands' one gate, `scaffold.RequireSupported`,
reads the pin as a `specver.Version`, and no 0.x minor is left in `scaffold`;
`migrate` names its own target, 0.7, and refuses a 1.0 pin before the pre-0.4
chain can run; and `scaffold.ListingTarget` reads a listing's first link with
`links.Find`, so one whose link has a title or a `<…>` destination is found.

Plan 4 did its item: a link from the bundle root whose target leaves the new
base stays relative once `links.Retarget` rewrites it, as spec 1.0's
*Cross-linking* writes a link to a file outside the bundle, and a test holds
it there, beside one showing such a link unchanged while the base stays put.

## Carried forward from Plan 2

Plan 3 did its items: every command writes 1.0 through `layout`; `ReservedDirs`
and its test went with the 0.x layout, and `TestNewRefusesWhatTheValidatorRejects`
holds `fdf new` to the validator; the moves `fdf lexicon` suggests are ones
`fdf mv` makes; the conformance harness finds a `repo/` fixture's bundle as fdf
does, and `resource-missing-v10` tests R1; `fdf migrate` refuses a pin at or
above 1.0; and `spec/1.0.md` settles that a directory that holds no Markdown,
at any depth, is outside FDF in a register as at the root. F3 already read it
so, F12 (`groupName`) now does, and such a directory, misnamed or not, is never
reported.

Plan 4 did its items: `links.Find` reads all five of the gaps Plan 2 found
as CommonMark does — (a) a fence marker indented four columns opens no fence,
(b) a heading ends its block, (c) a destination's balanced parentheses and
escapes are part of it, (d) a reference definition that would interrupt a
paragraph, an escaped bracket and one in a code span are text, and (e) a line
indented four columns after a heading or a closing fence is code — and every
fixture's `fdf validate` output is as it was.

Plan 5 did its items: the validator checks 1.0 alone, so a pin it does not
support, or none, is an F1 error that names the fix and ends validation, and
the "validated under v0.2 rules" warning went with `visitV0`, the version
gates, `linkRe`, `sectionLinks`, `dirName`, `registerDirs` and the 0.6
`checkDomain`; the fixtures lost their `-vNN` suffixes; `SPEC.md` and
`spec/README.md` list 1.0 as current; F6 and F7 read a reference-style link
where it is used (`links.Rendered`), so a lone definition lists nothing; a
task at a register's root is told that a task sits in the task directory
beside what it belongs to; and a document filed in a feature's task
directory is told whose task directory it is in.

## Carried forward from Plan 3

Plan 4 did its items: `fdf migrate` takes any 0.x bundle to 1.0 (`target`),
and a bundle already at 1.0 takes design §6's repair path. It keeps its
refusal of a root inside a pinned bundle, refuses a document named
`index.md` or `log.md`, and moves a v0.6 `bugs/` feature group to
`features/bugs/`, so `bugsGroupConflict` went. Migrate plans its indexes as
text (`scaffold.IndexText`, `ContextStub`, `SpecDoc`, `RegisterLine`,
`WithRegisterListing`), so the writers only it used, `EnsureChangesIndex`
and its three siblings, `RefreshSpec` and `EnsureContextStubs`, went; the
migrated root index lists the registers as `fdf init`'s does. It also
folded in the item Plan 3's final review parked: `fdf init` refuses a root
inside a pinned bundle, in `fdfroot.InsideBundle`'s words.

Plan 5 did its items: `fdf install` writes 1.0's skills and primer, naming
`docs/fdf` unless given another root, and recognizes an untouched primer
written for the root a marker records or for `docs/features`; the tests that
show a command refusing a 0.x bundle pin 0.7 in a copy of a 1.0 one
(`pinTo`); `main.version` reads 1.0.0, and a test holds it, `install.Version`
and the plugin manifest to the current spec; a test holds `layout`'s Context
documents and registers to the current spec's *Casing* section, the tripwire
that went with `ReservedDirs`; `fdfroot.Pin` is the one reader of a pin,
which the validator, the commands, root resolution and migrate share, and
root frontmatter that does not parse is F1; F1 tells a pin that is not a
`MAJOR.MINOR` version to be corrected; the README says where fdf looks;
`fdf mv` takes a group named `index` or `log` (`PlaceGroup`); `fdf init`
restores the `SPEC.md` of the version the bundle pins; and `fdf log` asks the
gate before it reads the bundle.

## Carried forward from Plan 4

Plan 5 did its items: `fdf install` teaches 1.0 (Plan 3's first item); the
0.x fixtures were migrated and finished by hand, or removed where 1.0 has no
place for their premise; `fdf mv` leaves a feature ID that begins a
`resource` or `applies-to` path as it is (`refactor.FieldPaths`, which
migrate shares); an unpinned bundle in the stem layout is asked for the pin
it was written for; the undo and the back-out remove the directories migrate
made, with what git ignores in them (`made`, `parents`); `--skip`; the
listing of a symbolic link the move breaks that migrate cannot name again,
one whose target is absolute or one outside the bundle;
the refusal of a register's place taken by a file (`registerFile`), in the
1.0 repair too; `stripStatusTags` reads code as `links` does; and the
back-out test covers a bundle that is its own repository. The fixtures'
conversion met migrate's exit code; ginee's errors, below, are its own 0.7
work.

## Plan 5's final review

The review of the whole branch found no critical defect, and these fixed
before release: migrate rewrites a pin between `---` lines with spaces
around them, where `fdfroot.Pin` reads it, rather than add a second block;
the pin errors of the validator and the commands name the upgrade as the
user's decision, with its dry run (`fdfroot.Upgrading`), since 0.7's skills
tell an agent to run `fdf migrate` whenever a pin is not supported; a 0.x
pin FDF never had is corrected, not sent to migrate (`specver.Known0x`); a
`docs/features` beside `docs/fdf` whose `INDEX.md` pins nothing is named
when no bundle is found, and `fdf init` refuses the place migrate moves it
to (`fdfroot.Beside`); the README and fdf-help carry `--skip` from the dry
run into the real one; and the primer names the root's optional
`README.md`.

## After 1.0

The README lists the first seven as what `fdf migrate` 1.0.0 leaves to the
user.

- Outside a git repository, migrate rewrites no mention of the bundle's path,
  inside the bundle or out: without a project root it cannot tell the
  bundle's path.
- A monorepo component's own mention of its bundle, `docs/features` in
  `services/api/README.md` for a bundle at `services/api/docs/features`, is
  not the bundle's path from the project root, so migrate neither rewrites
  it nor lists it. Nor does it reach the superproject of a bundle below a
  submodule's root.
- A group with no index, which the root never listed, gets a listing of its
  directory in `features/INDEX.md`, and no index of its own. A `releases/`
  with no `INDEX.md` gets none either, and the root lists it only once
  `fdf release` writes one.
- A group the root lists in an ordered list or a table is no listing, as
  listings are bullets: its line stays in the root `INDEX.md`, its link
  repaired, and `features/INDEX.md` gets a generated listing without its
  description.
- Inside the bundle, a mention of a group's path that is no feature ID, such
  as `venues/INDEX.md` in prose, is not rewritten: only IDs, links and the
  bundle's path are.
- The clean-tree check and the printed undo put every outside file migrate
  rewrites on one command line, which a very large monorepo could push past
  the system's argument-length limit. The check then fails safely, before
  anything is written; `--pathspec-from-file` would scale.
- The check for a place git would ignore reads each `.gitignore` where it is
  before anything moves, so the rules of one inside the bundle are not read
  where the bundle takes them: a file git tracks that such a rule ignores
  (`*.png` in `docs/features/.gitignore`, with a diagram force-added) still
  drops out of git once it moves.
- Migrate's minor-version path (1.0 to 1.x: move the pin, re-vendor
  `SPEC.md`, create any new register's `INDEX.md`, edit no document, design
  §4) waits for 1.1; today it repairs a bundle at `target` only.

Plan 5 left these too:

- `fdf install` names `docs/fdf` in the skills even where the bundle
  resolves to `docs/features`, as one migrated with `--to docs/features`
  does; `--root` names it, as the README says.
- Only an `INDEX.md` spelled so marks a `docs/features` beside `docs/fdf`
  as a bundle from before 1.0 (`fdfroot.Beside`): a v0.1 bundle's
  `index.md`, or Markdown with no index, is not named, since a
  documentation site's `docs/features` holds Markdown too. The README says
  how to upgrade either.
- Migrate's report counts what it left by why, in the singular: "4
  mentions in a skipped file", "in a URL", "in a link that leads
  elsewhere", however many files or URLs they are in.
- An empty `INDEX.md`, which the README says will do for a bundle that had
  none, comes out of migrate pinned and listing the registers, with no
  title.
- `stemTrail` looks one level into the groups, so an unpinned bundle in the
  stem layout whose only trails are deeper, in a nested practice, debt or
  bug group, gets the older layouts' pre-flight refusal rather than the
  request for its pin. Either way nothing is written.
- `links.Rendered` refuses a reference link whose text holds balanced
  brackets (`[See [note] here][label]`), which CommonMark reads as a link,
  so F6 and F7 can miss one; 0.7 read no reference links at all.
- The validator prints its warnings in the order it meets them, and it
  meets features in a map's order, so two runs on one bundle can order some
  warnings differently; its errors are sorted. Every release since 0.2 has
  done so.
- Tests to write: `findMade`'s branch for a `features/` that is there
  already, and the parents of a submodule's `--to`, both of which the
  back-out's `git clean -fdx` reads; the refusal of a file in the place of
  `changes/` or `bugs/`, which one loop over the registers makes, by name;
  and a single "Current" row in the spec indexes.

## Acceptance on ginee

On 2026-09-26, Plan 5's build of fdf 1.0.0 migrated a `git clone
--no-hardlinks` of ginee, a 0.6 bundle at `docs/features/`:

- The dry run planned 26 features in 275 files into `features/`; 104 feature
  ID mentions in 25 documents, with 18 in logs kept as they are; 399 links in
  91 files; 61 mentions of the bundle's path in 29 documents; and, outside
  the bundle, 344 mentions in 138 files and 65 links in 18 files. It listed
  the 2 mentions it left, each after a longer path.
- Migrate moved the bundle to `docs/fdf/`. `git diff -M` shows 510 files: 365
  renames, 141 files edited outside the bundle, `SPEC.md` re-vendored (a
  delete and an add), and `features/INDEX.md` and `bugs/INDEX.md` new.
- It validated 356 documents, 26 features and 1 release: 299 errors, 287 F8
  and 12 F10, all from what 0.7 added (a test case is a
  `## <scenario name>` heading, and F10 reads a Fix's regression cases
  there), and 46 warnings. Migrate exits 1 on them, as designed (Decision
  19); they are ginee's 0.7 work.
- Against the design's measures (*Testing*): before, fdf 0.7.0 validated the
  unmigrated clone, pinned 0.6, with no errors and no warnings. After, there
  is no broken-link warning, so none more than before, and `git diff -M`
  shows renames rather than rewrites. "No new errors" is not met as written:
  every one of the 299 is a rule 0.7 added, which ginee's 0.6 pin never had
  to meet, and migrate changes no document to meet a newer rule. So are the
  46 warnings: 0.7's lexicon scan, surface check and test-case names, and one
  placeholder left in a group index.
- `fdf install --project claude-code` then installed the ten skills and added
  the primer, and `fdf history`, `fdf mv --dry-run`, `fdf debt --open` and
  `fdf lexicon` read the migrated bundle.
