# FDF 1.0 — delivery roadmap

**Spec:** [2026-09-24-fdf-v1.0-design.md](../specs/2026-09-24-fdf-v1.0-design.md)

The 1.0 design spans five subsystems that depend on each other in order: the
foundations, the validator, the commands, `fdf migrate`, and the install,
skills and documentation. It is delivered as five plans, each written in full
once the one before it has landed, because each plan builds on the exact APIs
its predecessor creates.

The plans **expand, then contract**. Plans 1 to 4 add 1.0 beside 0.x, so every
step leaves the tests green; Plan 5 removes 0.x and ships. No release is cut
from the integration branch until Plan 5 is done.

## Branches

- **`v1`** is the integration branch, created from `claude/v1-0-design` (which
  holds the design, this roadmap and the plans) once they are reviewed.
- **Each plan** is worked on a branch off `v1` and merged into `v1` by pull
  request, once CI passes (`go vet ./...`, `go test ./...`, `gofmt -l .`
  printing nothing, and the skills' frontmatter lint).
- **0.7.1** fixes `fdf mv` for everyone still on 0.7, and is released from
  `main`. Plan 1 is done, so 0.7.1 is five of its commits cherry-picked onto a
  branch off `main`: ff7e809 (the `links` engine), 40635f6 (`fdf mv` on it),
  51abc22 (`fdf log` reads the pin as the validator does), 20bce8f
  (angle-bracket destinations) and 1546a13 (a listing found by its first
  link). None needs `specver`. Only CLAUDE.md conflicts, on ff7e809: put the
  `links` bullet after the `fdfroot` bullet. They fix these defects in 0.7.0:
  - links that leave the bundle break on a move that changes depth;
  - a footnote's first word is rewritten as a path;
  - a reference definition inside a code fence is rewritten;
  - on a moved index listing line, URLs, anchors and mail links are rewritten
    as paths;
  - a destination written as `<path>` loses its opening bracket to the path;
  - a listing line that links on to other documents stays behind when its
    document changes group, and another document's listing that mentions the
    moved one is carried off in its place;
  - `fdf log` refuses a draft's log under a single-quoted pin that
    `fdf validate` accepts.

  When 0.7.1 lands, merge `main` into `v1`. Only CLAUDE.md conflicts: keep
  `v1`'s side.
- **When Plan 5 is done**, `v1` merges into `main` and `v1.0.0` is tagged
  there.

## The plans

| # | Plan | Delivers | Needs |
|---|---|---|---|
| 1 | [Foundations](2026-09-24-fdf-v1.0-plan-1-foundations.md) | `specver` (MAJOR.MINOR versions, compared by number); `links`, the one link-repair engine; `fdf mv` on the engine, which fixes its broken links on moves that change depth | — |
| 2 | [Validator learns 1.0](2026-09-24-fdf-v1.0-plan-2-validator.md) | `spec/1.0.md`; the `layout` package; `1.0` among the supported pins; for a 1.0 pin, positions from `layout` (the closed root, `features/`, nested groups, flat features, the directory rule), the did-you-mean hint (F10, F14), `features` among F12's register names, and links read by `links.Find`; the 1.0 fixtures. 0.x validation unchanged | 1 |
| 3 | [Commands write 1.0](2026-09-25-fdf-v1.0-plan-3-commands.md) | `CurrentVersion` 1.0 and `fdf init`; root resolution (`docs/fdf/`, and also `docs/features/`); `new`, `adopt`, `practice`, `debt`, `bug`, `change`, `fix` on `layout`, with nested groups and name clashes refused; full feature IDs in `--affects` and `history`; `mv`, `lexicon`, `log`, `release`, the registers' `--cleanup` and `adopt`'s map on `layout`; `help` and `spec`. Commands point a 0.x bundle at `fdf migrate` | 2 |
| 4 | `fdf migrate` 0.x → 1.0 | Design §6 in full: the plan computed first, `--dry-run`, feature groups into `features/`, IDs, links, indexes, pin, relocation and `--to`, references outside the bundle, the refusals, the log, the report and the next steps | 1, 2, 3 |
| 5 | Contract and ship | The validator drops 0.x (version gates removed; the 0.x fixtures converted with migrate or removed; the pin required; the soft-check changes); `fdf install` (the primer, `legacyPrimers`, recognizing the root recorded in `.fdf-version`); the ten skills; `README.md`, `SPEC.md`, `spec/README.md`, this repository's `CLAUDE.md` and `embed.go`; acceptance on a scratch clone of ginee; release 1.0.0 | 4 |

## Where each part of the design lands

| Design section | Plan |
|---|---|
| §1 Layout, §2 IDs | 2 (validator), 3 (commands) |
| §3 Location and root resolution | 3 |
| §4 Versioning | 1 (comparison by number), 2 (1.0 pin), 5 (0.x dropped, pin required) |
| §5 Link repair | 1 (engine, `fdf mv`), 2 (the validator's link check) |
| §6 `fdf migrate` | 4 |
| §7 CLI | 2 (`layout`), 3 (commands), 5 (`install`) |
| §8 Skills and docs | 2 (`spec/1.0.md`), 5 (everything else) |
| Rule changes | 2 (for 1.0 pins), 5 (for every pin) |
| Testing | each plan its own; 5 converts the fixtures and runs the ginee acceptance |

## Carried forward from Plan 1

Plan 1's reviews left these for the plans that own them.

Plan 2 did its items: `links.Find` reads every listed form, `links.Resolve` is
exported and shared, `bundle.pinAtLeast` takes a `specver.Version`, and every
`supportedVersions` key is tested to parse.

**Plan 3 (commands):**
- `pinAtLeast(pin, minor)` in `scaffold` still takes a 0.x minor. The first 1.x
  gate there needs a `specver.Version`, as `bundle`'s now takes.
- `migrate` treats only `0.4` to `0.6` as the stem layout, and relies on
  `pin == currentVersion` for 0.7. Once `CurrentVersion` is 1.0, a 0.7 bundle
  would run the pre-0.4 chain: guard it until Plan 4 replaces migrate.
- `scaffold.ListingTarget` needs `)` right after the target, so a listing
  whose own link has a title is not found by `Unlist`, `Listed`, `ListedIn` or
  `fdf mv`. fdf never writes one; decide when the commands move onto
  `layout`.

**Plan 4 (`fdf migrate`):**
- `links.Retarget` turns a link from the bundle root whose target leaves the
  new base into a relative link. Decide whether that is wanted, and test it.

## Carried forward from Plan 2

**Plan 3 (commands):**
- `spec/1.0.md` is embedded, so `scaffold` counts a 1.0 pin as supported, and
  under one the commands still write the 0.7 layout. Only fixtures pin 1.0
  until the commands move onto `layout`.
- `TestReservedDirsMirrorTheValidator` holds `ReservedDirs` to the validator
  for the five 0.x reserved directories only. Under 1.0, `features/` and the
  closed root belong in that check once `ReservedDirs` comes from `layout`.
- `fdf lexicon` finds a 1.0 bundle's names through `ScanBundle`, which reads
  the pin and asks `layout`; its `--fix` and the names it suggests still
  assume the 0.7 layout.
- The conformance harness finds a `repo/` fixture's bundle at
  `repo/docs/features`, so no 1.0 fixture tests R1 yet. One can once root
  resolution knows `docs/fdf/`.
- `fdf migrate` does nothing only when the pin equals `currentVersion`. On a
  conformant 1.0 bundle its 0.x pre-flight lets through, such as a flat one
  whose features have no trail yet, it rewrites the pin to 0.7, copies in
  the 0.7 `SPEC.md` and logs a migration; on others, such as `valid-v10`,
  the pre-flight refuses, with messages about v0.4. Plan 2's validator is
  the first to call such a bundle conformant, and Plan 3's commands write
  one: `migrate` must refuse a pin at or above 1.0 until Plan 4 replaces it.
- `spec/1.0.md` is ambiguous about a directory in a register that holds no
  Markdown. Its closed-root rule says such a directory is outside FDF, but
  the directory rule, F3(c) and migrate's refusals make any directory beside
  a practice, debt or bug an error. The validator accepts
  `debts/x/diagram.png` beside `debts/x.md`, and F3 ignores a group that
  holds only images, such as `features/shop-images/`, while F12 still
  reports a banned word in its name. Settle one rule in `spec/1.0.md`, and
  make F3 and F12 (`groupName`) follow it. The rule also decides whether a
  misnamed nested directory that holds no Markdown is reported: today it
  never is.

**Plan 4 (`fdf migrate`):**
- `links.Find` has five known gaps. (a) and (c) must be fixed before
  `fdf migrate` runs the engine over a whole project, where every tracked
  text file passes through it.
  - (a) A fence marker indented four spaces or more still opens a fence,
    where CommonMark reads the line as indented code, because `Find` tests
    for a fence before it tests for indented code; if nothing closes it, the
    rest of the text counts as code.
  - (b) A heading does not end its block for code-span pairing, so a stray
    backtick in a heading can hide a link on the next line.
  - (c) Balanced parentheses in a destination are misread:
    `[a](five(1).md)` gives the target `five(1`.
  - (d) A reference definition that interrupts a paragraph, an escaped `\[`,
    and a `[` inside a code span are each read as a link.
  - (e) A line indented four columns right after a closing fence or a
    heading is read as prose, where CommonMark reads an indented code block,
    because `Find` wants a blank line before indented code. F6 and F7 now
    follow `Find` there, as the broken-link check does.

**Plan 5 (contract and ship):**
- A pin the validator does not support is still validated under v0.2's rules
  after its F1 error. With 0.x gone, validation stops at F1.
- A bundle with no pin is still validated under v0.2's rules, with a
  warning. With 0.x gone, a missing pin is an F1 error, and the "validated
  under v0.2 rules" warning goes (design §4).
- The 0.x code to remove: `visitV0` in `validate.go`; the `stem`, `v5`, `v6`
  and `v7` gates in `collection` and in the checks; `linkRe` and the 0.x
  branches of `resolveLink` and `sectionLinks`; `dirName`, `registerDirs` and
  `checkDomain`.
- The 1.0 fixtures carry a `-v10` suffix, which goes with every other `-vNN`.
- `spec/README.md` and `SPEC.md` do not list `spec/1.0.md` yet.
- F6 and F7 place a reference-style link by where its definition sits
  (`sectionTargets`): `[01-signup-form.md][signup]` in `# Tasks`, with the
  definition at the end of the plan, gives a false F6, and a lone definition
  inside the section, which renders nothing, counts as a listing. 0.x
  ignored reference links entirely, so this is not a regression.
- An orphan task at a register root gets a misleading message
  (`documentV1` in `walk.go`): `features/01-api.md`, typed Task, is told
  that `features.md` does not exist, a file the closed root forbids.
- A group named like a flat feature, `features/onboarding/` beside
  `features/onboarding.md`, is that feature's task directory, so the
  documents filed in it are reported only as "task directories may contain
  only NN-slug.md tasks". The message could name the clashing
  `features/onboarding.md`.
