---
description: Initialize an FDF bundle and run the fdf-init context interview
---

Run `fdf init` (respecting `--root`/`FDF_ROOT_DIR`), then use the fdf-init
skill to fill the three Context documents (STACK.md, ARCHITECTURE.md,
INFRA.md) through the project-context interview. Skip the skill's own scaffold
step since `fdf init` already ran.
