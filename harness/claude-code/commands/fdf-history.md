---
description: Show a delivered FDF feature's post-delivery changes and fixes
---

Run `fdf history $ARGUMENTS` and summarize what has happened to that feature
since it shipped — which changes altered its behavior, which fixes corrected
drift, and what is still open. The list is computed from each document's
`affects:`, so it needs no back-links on the feature itself.
